---
import Base from "./Base.astro";

const {title, description} = Astro.props.frontmatter || Astro.props;
---

<Base title={title} description={description} >
  <main class="prose max-w-x mx-auto py-32 px-10">
    <slot />
  </main>
</Base>
